<div class="mt-4">
   <div class="row">
      <div class="col-lg-3">
         <button type="button" class="btn btn-primary">Học ngay</button>
      </div>

         <div class="col-lg-9">
            <h2>{{Course.name}}</h2>
            <iframe width="560" height="330" src="https://www.youtube.com/embed/{{Course.youtubeID}}" title="Course detail page" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p>{{Course.description}}</p>
         </div>
   </div>
</div>